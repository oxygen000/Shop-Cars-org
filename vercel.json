{
  "version": 2,
  "buildCommand": "npx nx build shop && npx nx build admin && mkdir -p dist/shop dist/admin && cp -r apps/shop/dist/* dist/shop/ && cp -r apps/admin/dist/* dist/admin/",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/admin/(.*)", "destination": "/admin/$1" },
    { "source": "/(.*)", "destination": "/shop/$1" }
  ]
} 